---
import BaseLayout from "../layouts/BaseLayout.astro";
import {
	WHATSAPP_CTA,
	CALENDLY_ARTISTAS,
	MP_ASESORIA_ARTISTAS,
} from "../data/constantes";
---

<BaseLayout
	title="Artistas"
	description="Estés empezando, lanzando o buscando crecer, aquí encuentras la estructura y el impulso que tu música necesita."
>
	<!-- SECCIÓN 1 — HERO -->
	<section class="hero" aria-label="Hero artistas">
		<div class="hero__bg">
			<img
				src="/images/HeroArtistas.jpeg"
				alt="Artistas musicales con dirección"
				class="hero__img"
				fetchpriority="high"
			/>
			<div class="hero__overlay"></div>
		</div>

		<div class="container hero__content">
			<h1 class="display display--xl hero__title reveal reveal-delay-1">
				TU <span class="gradient-text">MÚSICA</span> NECESITA<br />
				MÁS QUE TALENTO,<br />
				NECESITA <span class="gradient-text">DIRECCIÓN</span>
			</h1>

			<p class="body--lg hero__sub reveal reveal-delay-2">
				Estés empezando, lanzando o buscando crecer, aquí encuentras la
				estructura y el impulso que tu música necesita.
			</p>

			<div class="hero__ctas reveal reveal-delay-3">
				<a href="#soluciones" class="btn hero__btn">
					Ver cómo impulsar mi música
				</a>
				<a
					href={CALENDLY_ARTISTAS}
					target="_blank"
					rel="noopener noreferrer"
					class="btn hero__btn"
				>
					Agendar asesoría estratégica
				</a>
			</div>

			<a
				href="#soluciones"
				class="hero__scroll reveal reveal-delay-4"
				aria-label="Ver soluciones para artistas"
			>
				<span>Conocer más</span>
				<svg
					width="16"
					height="16"
					viewBox="0 0 16 16"
					fill="none"
					aria-hidden="true"
				>
					<path
						d="M8 3v10M3 8l5 5 5-5"
						stroke="currentColor"
						stroke-width="1.5"
						stroke-linecap="round"
						stroke-linejoin="round"></path>
				</svg>
			</a>
		</div>
	</section>

	<div class="section-line"></div>

	<!-- SECCIÓN 2 — INTRO -->
	<section
		class="intro"
		id="soluciones"
		aria-label="Estructura para artistas"
	>
		<div class="container intro__inner">
			<h2 class="display display--lg intro__titulo reveal reveal-delay-1">
				ESTRUCTURA REAL PARA HACER<br />
				QUE TU <span class="gradient-text">MÚSICA</span> AVANCE
			</h2>

			<div class="intro__divider reveal reveal-delay-2"></div>

			<p class="intro__texto reveal reveal-delay-2">
				No todos los artistas están en el mismo punto. Algunos están
				comenzando, otros ya tienen equipo y otros trabajan desde una
				disquera. Pero todos tienen algo en común: si no hay ejecución
				real, la música no avanza.
			</p>

			<p class="intro__cta reveal reveal-delay-3">
				<em>Aquí es donde entramos.</em>
			</p>
		</div>
	</section>

	<div class="section-line"></div>

	<!-- SECCIÓN 3 — DIFERENTES ETAPAS -->
	<section class="etapas-sec" aria-label="Diferentes etapas">
		<div class="container">
			<div class="etapas__header reveal">
				<span class="label">El patrón que se repite</span>
				<h2 class="display display--lg etapas__titulo">
					DIFERENTES ETAPAS,<br />LOS MISMOS <span
						class="gradient-text">BLOQUEOS</span
					>
				</h2>
			</div>

			<div class="etapas__foto reveal">
				<img
					src="/images/InternaArtistas01.jpeg"
					alt="Artistas en diferentes etapas"
					loading="lazy"
				/>
			</div>

			<div class="etapas__grid">
				<div class="etapa-card reveal">
					<div class="etapa-card__header">
						<img
							src="/images/ArtistasEmergentes.png"
							alt=""
							class="etapa-card__icono"
							aria-hidden="true"
						/>
						<h3 class="display display--sm etapa-card__titulo">
							Artistas<br />emergentes
						</h3>
					</div>
					<ul class="etapa-card__lista">
						<li>Sacan música, pero nadie la escucha</li>
						<li>No saben cómo lanzar correctamente</li>
						<li>Todo depende de "subir contenido"</li>
					</ul>
				</div>
				<div class="etapa-card reveal reveal-delay-1">
					<div class="etapa-card__header">
						<img
							src="/images/ArtistasCrecimiento.png"
							alt=""
							class="etapa-card__icono"
							aria-hidden="true"
						/>
						<h3 class="display display--sm etapa-card__titulo">
							Artistas en<br />crecimiento
						</h3>
					</div>
					<ul class="etapa-card__lista">
						<li>Tienen equipo, pero no hay dirección clara</li>
						<li>Invierten, pero no ven resultados consistentes</li>
						<li>Su contenido no escala</li>
					</ul>
				</div>
				<div class="etapa-card reveal reveal-delay-2">
					<div class="etapa-card__header">
						<img
							src="/images/EquiposDisqueras.png"
							alt=""
							class="etapa-card__icono"
							aria-hidden="true"
						/>
						<h3 class="display display--sm etapa-card__titulo">
							Equipos y<br />disqueras
						</h3>
					</div>
					<ul class="etapa-card__lista">
						<li>Falta de ejecución operativa</li>
						<li>Procesos desordenados en lanzamientos</li>
						<li>Necesidad de apoyo externo confiable</li>
					</ul>
				</div>
			</div>
		</div>
	</section>

	<div class="section-line"></div>

	<!-- SECCIÓN 4 — SERVICIOS -->
	<section class="servicios-sec" aria-label="Servicios para artistas">
		<!-- Header sección 4 -->
		<div class="servicios-header">
			<div class="container">
				<div class="reveal">
					<h2 class="display display--lg">
						NUESTROS <span class="gradient-text">SERVICIOS</span>
					</h2>
				</div>
			</div>
		</div>

		<!-- 4A — FRASE PIVOTE -->
		<div class="pivote">
			<div class="container">
				<p class="pivote__texto reveal">
					<span class="gradient-text"
						>ESTO ES LO QUE HACEMOS CUANDO HAY DIRECCIÓN:</span
					>
				</p>
			</div>
		</div>

		<!-- 4B — SERVICIO 1: LANZAMIENTO (texto + imagen) -->
		<div class="servicio-bloque servicio-bloque--imagen-derecha">
			<div class="container servicio-bloque__inner">
				<div class="servicio-bloque__texto reveal">
					<span class="servicio-num">01</span>
					<h3 class="display display--md servicio-titulo">
						LANZAMIENTO Y ESTRATEGIA DIGITAL
					</h3>
					<p class="servicio-desc">
						Diseñamos y ejecutamos lanzamientos musicales según tu
						etapa: desde artistas emergentes hasta equipos y
						disqueras que necesitan apoyo operativo.
					</p>
					<div class="servicio-items">
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Estrategias de lanzamiento</span
							>
							<p class="servicio-item__desc">
								Creamos el plan completo para lanzar tu tema con
								dirección y sentido.
							</p>
						</div>
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Planeación de campañas musicales</span
							>
							<p class="servicio-item__desc">
								Organizamos cada acción del lanzamiento para
								generar impacto real.
							</p>
						</div>
					</div>
					<a
						href={CALENDLY_ARTISTAS}
						target="_blank"
						rel="noopener noreferrer"
						class="btn servicio-btn"
						>Quiero lanzar mi música correctamente</a
					>
				</div>
				<div class="servicio-bloque__imagen reveal reveal-delay-2">
					<img
						src="/images/InternaArtistas01.jpeg"
						alt="Lanzamiento musical con estrategia"
						loading="lazy"
					/>
				</div>
			</div>
		</div>

		<!-- 4C — SERVICIO 2: VISIBILIDAD (ancho completo) -->
		<div class="servicio-bloque servicio-bloque--full">
			<div class="container">
				<div class="servicio-full__header reveal">
					<span class="servicio-num">02</span>
					<h3 class="display display--md servicio-titulo">
						VISIBILIDAD DIGITAL Y PLATAFORMAS
					</h3>
					<p class="servicio-desc servicio-desc--wide">
						Llevamos tu música a más personas con estrategias
						enfocadas en alcance, tráfico y posicionamiento real.
					</p>
				</div>
				<div class="servicio-full__grid">
					<div class="servicio-item servicio-item--card reveal">
						<span class="servicio-item__nombre"
							>Campañas de promoción en YouTube</span
						>
						<p class="servicio-item__desc">
							Impulsamos tus videos con vistas segmentadas o
							latinas para aumentar alcance real.
						</p>
					</div>
					<div
						class="servicio-item servicio-item--card reveal reveal-delay-1"
					>
						<span class="servicio-item__nombre"
							>Estrategias de crecimiento en redes sociales</span
						>
						<p class="servicio-item__desc">
							Desarrollamos acciones para que tu contenido crezca
							con intención y consistencia.
						</p>
					</div>
					<div
						class="servicio-item servicio-item--card reveal reveal-delay-2"
					>
						<span class="servicio-item__nombre"
							>Campañas con influencers en TikTok e Instagram</span
						>
						<p class="servicio-item__desc">
							Conectamos tu música con creadores que amplifican tu
							visibilidad.
						</p>
					</div>
					<div
						class="servicio-item servicio-item--card reveal reveal-delay-3"
					>
						<span class="servicio-item__nombre"
							>Promoción en páginas temáticas y urbanas</span
						>
						<p class="servicio-item__desc">
							Publicamos tu tema en plataformas que ya tienen
							audiencia activa en tu género.
						</p>
					</div>
				</div>
				<div class="servicio-full__cta reveal">
					<a
						href={CALENDLY_ARTISTAS}
						target="_blank"
						rel="noopener noreferrer"
						class="btn servicio-btn"
						>Quiero que mi música llegue a más personas</a
					>
				</div>
			</div>
		</div>

		<!-- 4D — SERVICIO 3: PRODUCCIÓN (imagen + texto) -->
		<div class="servicio-bloque servicio-bloque--imagen-izquierda">
			<div class="container servicio-bloque__inner">
				<div class="servicio-bloque__imagen reveal">
					<img
						src="/images/InternaArtistas02.jpeg"
						alt="Producción audiovisual para artistas"
						loading="lazy"
					/>
				</div>
				<div class="servicio-bloque__texto reveal reveal-delay-1">
					<span class="servicio-num">03</span>
					<h3 class="display display--md servicio-titulo">
						PRODUCCIÓN AUDIOVISUAL
					</h3>
					<p class="servicio-desc">
						Fortalecemos tu imagen artística con contenido visual
						profesional, tanto para artistas independientes como
						para proyectos más estructurados.
					</p>
					<div class="servicio-items">
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Producción de video oficial</span
							>
							<p class="servicio-item__desc">
								Creamos videos que representan tu música con
								calidad y dirección.
							</p>
						</div>
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Producción musical</span
							>
							<p class="servicio-item__desc">
								Trabajamos el sonido de tus temas para lograr un
								resultado profesional.
							</p>
						</div>
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Fotografía profesional</span
							>
							<p class="servicio-item__desc">
								Construimos una imagen visual sólida para tu
								proyecto.
							</p>
						</div>
						<div class="servicio-item">
							<span class="servicio-item__nombre"
								>Contenido visual para redes</span
							>
							<p class="servicio-item__desc">
								Generamos piezas listas para publicar y conectar
								con tu audiencia.
							</p>
						</div>
					</div>
					<a
						href={CALENDLY_ARTISTAS}
						target="_blank"
						rel="noopener noreferrer"
						class="btn servicio-btn"
						>Quiero elevar mi proyecto visual</a
					>
				</div>
			</div>
		</div>

		<!-- 4E — SERVICIO 4: MEDIOS (ancho completo) -->
		<div class="servicio-bloque servicio-bloque--full">
			<div class="container">
				<div class="servicio-full__header reveal">
					<span class="servicio-num">04</span>
					<h3 class="display display--md servicio-titulo">
						MEDIOS Y PRESENTACIONES
					</h3>
					<p class="servicio-desc servicio-desc--wide">
						Abrimos espacios de exposición para artistas en
						crecimiento y en consolidación que necesitan presencia
						real en escenarios y medios.
					</p>
				</div>
				<div class="servicio-full__grid servicio-full__grid--3col">
					<div class="servicio-item servicio-item--card reveal">
						<span class="servicio-item__nombre">Gira de medios</span
						>
						<p class="servicio-item__desc">
							Gestionamos entrevistas y apariciones para darte
							exposición.
						</p>
					</div>
					<div
						class="servicio-item servicio-item--card reveal reveal-delay-1"
					>
						<span class="servicio-item__nombre"
							>Presentaciones en discotecas, colegios y eventos
							comunitarios</span
						>
						<p class="servicio-item__desc">
							Te conectamos con escenarios donde tu música puede
							crecer.
						</p>
					</div>
					<div
						class="servicio-item servicio-item--card reveal reveal-delay-2"
					>
						<span class="servicio-item__nombre"
							>Publicidad en salas de cine</span
						>
						<p class="servicio-item__desc">
							Proyectamos tu video musical en pantallas de alto
							impacto.
						</p>
					</div>
				</div>
				<div class="servicio-full__cta reveal">
					<a
						href={CALENDLY_ARTISTAS}
						target="_blank"
						rel="noopener noreferrer"
						class="btn servicio-btn"
						>Quiero más exposición para mi música</a
					>
				</div>
			</div>
		</div>

		<!-- 4F — FRASE FINAL -->
		<div class="cierre-sec">
			<div class="container">
				<p class="cierre-sec__texto reveal">
					<span class="gradient-text"
						>SI TU MÚSICA YA EXISTE, ES MOMENTO<br />DE HACERLA
						CRECER DE VERDAD</span
					>
				</p>
			</div>
		</div>
	</section>

	<div class="section-line"></div>

	<!-- SECCIÓN 5 — ASESORÍA ESTRATÉGICA -->
	<section
		class="asesoria-sec"
		aria-label="Asesoría estratégica para artistas"
	>
		<!-- 5A — DESCRIPCIÓN + IMAGEN -->
		<div class="asesoria-header">
			<!-- Texto -->
			<div class="asesoria-body">
				<div class="container">
					<div class="asesoria-body__intro reveal">
						<h2 class="display display--lg asesoria-titulo">
							ASESORÍA ESTRATÉGICA PARA <span
								class="gradient-text">ARTISTAS</span
							><br />QUE QUIEREN <span class="gradient-text"
								>AVANZAR EN SERIO</span
							>
						</h2>
						<p class="asesoria-desc">
							Sesión personalizada diseñada para analizar tu
							proyecto musical, evaluar tu situación actual y
							brindarte una orientación estratégica clara para el
							crecimiento de tu carrera.
						</p>
					</div>
					<div class="asesoria-listas reveal reveal-delay-1">
						<div class="asesoria-lista">
							<p class="asesoria-lista__titulo">
								Durante esta sesión trabajaremos en:
							</p>
							<ul>
								<li>Revisión de tu proyecto artístico</li>
								<li>Análisis de presencia digital</li>
								<li>Evaluación de tu posicionamiento actual</li>
								<li>
									Identificación de oportunidades de
									crecimiento
								</li>
							</ul>
						</div>
						<div class="asesoria-lista">
							<p class="asesoria-lista__titulo">Incluye:</p>
							<ul>
								<li>Diagnóstico previo del proyecto</li>
								<li>
									Orientación estratégica durante la sesión
								</li>
								<li>
									Informe posterior con recomendaciones
									generales <span class="asesoria-nota"
										>(Costo adicional, se indica en reunión)</span
									>
								</li>
							</ul>
						</div>
					</div>
				</div>
			</div>

			<!-- Imagen como bloque fotográfico independiente -->
			<div class="asesoria-foto reveal">
				<img
					src="/images/InternaArtistas04.jpeg"
					alt="Asesoría estratégica para artistas"
					loading="lazy"
				/>
			</div>
		</div>

		<!-- 5B — PRICING -->
		<div class="asesoria-pricing">
			<div class="container asesoria-pricing__inner">
				<div class="asesoria-pricing__item reveal">
					<span class="asesoria-pricing__label">Duración</span>
					<span class="asesoria-pricing__valor">60 minutos</span>
				</div>
				<div class="asesoria-pricing__divider"></div>
				<div class="asesoria-pricing__item reveal reveal-delay-1">
					<span class="asesoria-pricing__label">Modalidad</span>
					<span class="asesoria-pricing__valor">Virtual</span>
				</div>
				<div class="asesoria-pricing__divider"></div>
				<div class="asesoria-pricing__item reveal reveal-delay-2">
					<span class="asesoria-pricing__label">Inversión</span>
					<div class="asesoria-pricing__precios">
						<span>Colombia: <strong>$150.000 COP</strong></span>
						<span>Otro país: <strong>60 USD</strong></span>
					</div>
				</div>
			</div>
		</div>

		<!-- 5C — GUÍA DE PASOS -->
		<div class="asesoria-pasos">
			<div class="container">
				<!-- Paso 1: Pago -->
				<div class="asesoria-paso reveal">
					<div class="asesoria-paso__num">01</div>
					<div class="asesoria-paso__contenido">
						<h3 class="display display--sm asesoria-paso__titulo">
							Realizar el pago
						</h3>
						<p class="asesoria-paso__desc">
							Haz clic en el botón de pago y completa la
							transacción a través de MercadoPago. Aceptamos
							tarjetas de crédito, débito y otros medios
							disponibles en la plataforma.
						</p>
						<a
							href={MP_ASESORIA_ARTISTAS}
							target="_blank"
							rel="noopener noreferrer"
							class="btn asesoria-paso__btn"
						>
							Pagar asesoría estratégica
						</a>
					</div>
				</div>

				<!-- Paso 2: Correo -->
				<div class="asesoria-paso reveal">
					<div class="asesoria-paso__num">02</div>
					<div class="asesoria-paso__contenido">
						<h3 class="display display--sm asesoria-paso__titulo">
							Revisar tu correo electrónico
						</h3>
						<p class="asesoria-paso__desc">
							Una vez confirmado el pago, recibirás un correo con
							un enlace personal e intransferible para completar
							tu agendamiento. Revisa también tu carpeta de spam
							si no lo encuentras en los primeros minutos.
						</p>
						<div class="asesoria-paso__aviso">
							<span
								>Si el correo no llega, escríbenos por WhatsApp
								y te enviamos el enlace directamente.</span
							>
						</div>
					</div>
				</div>

				<!-- Paso 3: Formulario (ilustrativo) -->
				<div class="asesoria-paso reveal">
					<div class="asesoria-paso__num">03</div>
					<div class="asesoria-paso__contenido">
						<h3 class="display display--sm asesoria-paso__titulo">
							Completar el formulario previo
						</h3>
						<p class="asesoria-paso__desc">
							Desde el enlace del correo accederás a un formulario
							donde deberás completar la siguiente información
							para que podamos preparar tu sesión:
						</p>
						<div class="asesoria-form asesoria-form--preview">
							<div class="asesoria-form__campo">
								<span class="asesoria-form__label"
									>Nombre artístico</span
								>
								<div class="asesoria-form__input-preview">
									Tu nombre artístico
								</div>
							</div>
							<div class="asesoria-form__campo">
								<span class="asesoria-form__label"
									>Redes sociales</span
								>
								<div class="asesoria-form__input-preview">
									Usuarios de Instagram / TikTok
								</div>
							</div>
							<div class="asesoria-form__campo">
								<span class="asesoria-form__label"
									>Plataformas musicales</span
								>
								<div class="asesoria-form__input-preview">
									Usuarios de YouTube / Spotify
								</div>
							</div>
							<div class="asesoria-form__campo">
								<span class="asesoria-form__label"
									>Descripción del proyecto</span
								>
								<div
									class="asesoria-form__input-preview asesoria-form__input-preview--tall"
								>
									País, género, tiempo de carrera, logros o
									avances, entre otros
								</div>
							</div>
							<div class="asesoria-form__campo">
								<span class="asesoria-form__label"
									>Objetivos actuales</span
								>
								<div class="asesoria-form__input-preview">
									¿Qué quieres lograr con tu música?
								</div>
							</div>
						</div>
					</div>
				</div>

				<!-- Paso 4: Agendamiento -->
				<div class="asesoria-paso reveal">
					<div class="asesoria-paso__num">04</div>
					<div class="asesoria-paso__contenido">
						<h3 class="display display--sm asesoria-paso__titulo">
							Agendar tu sesión
						</h3>
						<p class="asesoria-paso__desc">
							Al enviar el formulario serás redirigido
							automáticamente al calendario para elegir la fecha y
							hora disponible que mejor se adapte a ti.
						</p>
					</div>
				</div>

				<!-- Paso 5: Confirmación -->
				<div class="asesoria-paso reveal">
					<div class="asesoria-paso__num">05</div>
					<div class="asesoria-paso__contenido">
						<h3 class="display display--sm asesoria-paso__titulo">
							Recibir confirmación
						</h3>
						<p class="asesoria-paso__desc">
							Una vez agendada la sesión recibirás un correo de
							confirmación con la fecha, hora y enlace de la
							videollamada. El equipo también quedará notificado y
							listo para atenderte.
						</p>
					</div>
				</div>
			</div>
		</div>

		<!-- 5D — NORMATIVAS -->
		<div class="asesoria-normativas">
			<div class="container">
				<div class="asesoria-normativas__inner reveal">
					<h3 class="display display--sm asesoria-normativas__titulo">
						Normativas
					</h3>
					<ul class="asesoria-normativas__lista">
						<li>
							La sesión debe agendarse posteriormente a realizar
							el pago, si no se pierde la oportunidad de programar
							su reunión.
						</li>
						<li>
							El tiempo de espera máximo el día de la sesión es de
							10 minutos.
						</li>
						<li>
							En caso de inasistencia sin aviso previo de mínimo
							12 horas, la inversión se pierde en su totalidad.
						</li>
						<li>La asesoría es individual y no transferible.</li>
					</ul>
				</div>
			</div>
		</div>

		<!-- 5E — CTA CIERRE -->
		<div class="asesoria-cierre">
			<div class="container asesoria-cierre__inner reveal">
				<p class="display display--lg asesoria-cierre__titulo">
					SI TE TOMAS TU MÚSICA EN SERIO,<br />ESTE ES EL <span
						class="gradient-text">SIGUIENTE PASO</span
					>
				</p>
				<a
					href={WHATSAPP_CTA}
					target="_blank"
					rel="noopener noreferrer"
					class="btn btn--primary"
				>
					Hablemos por WhatsApp
				</a>
			</div>
		</div>
	</section>
</BaseLayout>

<style>
	/* HERO */
	.hero {
		position: relative;
		min-height: 100svh;
		display: flex;
		align-items: flex-end;
		padding-top: 140px;
		padding-bottom: clamp(4rem, 8vw, 7rem);
		overflow: hidden;
	}

	.hero__bg {
		position: absolute;
		inset: 0;
	}

	.hero__img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center top;
	}

	.hero__overlay {
		position: absolute;
		inset: 0;
		background: linear-gradient(
			to left,
			rgba(8, 8, 16, 0.1) 0%,
			rgba(8, 8, 16, 0.7) 50%,
			rgba(8, 8, 16, 0.88) 100%
		);
	}

	.hero__content {
		position: relative;
		z-index: 1;
		display: flex;
		flex-direction: column;
		align-items: flex-start;
		gap: 1.5rem;
		max-width: 55%;
		margin-left: 8%;
		margin-right: auto;
	}

	@media (max-width: 768px) {
		.hero__content {
			max-width: 100%;
		}
	}

	.hero__title {
		line-height: 1.3;
	}

	.hero__sub {
		color: var(--text-high);
		font-size: 1.35rem;
		max-width: 48ch;
		font-weight: 500;
	}

	@media (max-width: 768px) {
		.hero__sub {
			font-size: 1.1rem;
		}
	}

	.hero__ctas {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		margin-top: 0.5rem;
	}

	.hero__btn {
		background: #fff;
		color: #000;
		border: none;
	}

	.hero__btn:hover {
		background: #fff;
		border: 2px solid var(--cyan);
		transform: translateY(-2px);
		background-clip: text;
		-webkit-background-clip: text;
		color: transparent;
		background-image: linear-gradient(90deg, var(--cyan), var(--magenta));
	}

	.hero__scroll {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 0.85rem;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--text-dim);
		margin-top: 1rem;
		transition: color var(--t-fast) var(--ease);
	}

	.hero__scroll:hover {
		color: var(--cyan);
	}

	/* INTRO */
	.intro {
		padding-block: var(--section-gap);
		background: var(--bg-surface);
	}

	.intro__inner {
		display: flex;
		flex-direction: column;
		align-items: center;
		text-align: center;
		gap: 1.75rem;
		max-width: 1060px;
		margin-inline: auto;
	}

	.intro__titulo {
		line-height: 1.05;
	}

	.intro__divider {
		width: 60px;
		height: 2px;
		background: linear-gradient(90deg, var(--cyan), var(--magenta));
		border-radius: 2px;
	}

	.intro__texto {
		font-size: clamp(1.26rem, 2.4vw, 1.5rem);
		color: var(--text-secondary);
		line-height: 1.8;
		font-weight: 400;
		max-width: 68ch;
	}

	.intro__cta {
		font-family: var(--font-display);
		font-size: clamp(1.3rem, 3vw, 1.9rem);
		letter-spacing: 0.06em;
		background: linear-gradient(
			135deg,
			#00cfdd 0%,
			#0a2fa8 35%,
			#e0004f 100%
		);
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
	}

	/* SECCIÓN 3 — SERVICIOS */
	.servicios-sec {
		background: var(--bg-base);
	}

	/* SECCIÓN 3 — ETAPAS */
	.etapas-sec {
		padding-block: var(--section-gap);
		background: var(--bg-base);
	}

	.etapas__header {
		text-align: center;
		margin-bottom: clamp(2.5rem, 5vw, 4rem);
	}

	.etapas__titulo {
		margin-top: 0.75rem;
		line-height: 1.05;
	}

	.etapas__foto {
		border-radius: var(--radius-lg);
		overflow: hidden;
		aspect-ratio: 21/9;
		margin-bottom: clamp(2rem, 4vw, 3rem);
	}

	.etapas__foto img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	@media (max-width: 900px) {
		.etapas__foto {
			aspect-ratio: 16/9;
		}
	}

	/* SECCIÓN 4 HEADER */
	.servicios-header {
		padding-block: clamp(3rem, 6vw, 5rem);
		background: var(--bg-base);
		text-align: center;
	}

	.etapas__grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.5rem;
	}

	.etapa-card {
		background: var(--bg-card);
		border: 1px solid rgba(0, 194, 255, 0.25);
		border-radius: var(--radius-lg);
		padding: clamp(1.5rem, 3vw, 2rem);
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
		transition: border-color var(--t-med) var(--ease);
	}

	.etapa-card:hover {
		border-color: var(--cyan);
	}

	.etapa-card__header {
		display: grid;
		grid-template-columns: 44px 1fr;
		align-items: center;
		column-gap: 0.85rem;
	}

	.etapa-card__icono {
		width: 44px;
		height: 44px;
		object-fit: contain;
	}

	.etapa-card__titulo {
		font-family: var(--font-body);
		font-weight: 700;
		color: var(--text-primary);
		font-size: clamp(1.1rem, 2vw, 1.4rem);
		text-transform: uppercase;
		letter-spacing: 0.04em;
		line-height: 1.3;
	}

	.etapa-card__lista {
		display: flex;
		flex-direction: column;
		gap: 0.6rem;
	}

	.etapa-card__lista li {
		font-size: 1.2rem;
		color: var(--text-secondary);
		padding-left: 1.2rem;
		position: relative;
		line-height: 1.5;
	}

	.etapa-card__lista li::before {
		content: "·";
		position: absolute;
		left: 0;
		color: var(--cyan);
		font-weight: 700;
	}

	/* FRASE PIVOTE */
	.pivote {
		padding-block: clamp(3rem, 6vw, 5rem);
		background: var(--bg-surface);
		border-top: 1px solid var(--border);
		border-bottom: 1px solid var(--border);
	}

	.pivote__texto {
		font-family: var(--font-display);
		font-size: clamp(1.4rem, 3.5vw, 2.4rem);
		letter-spacing: 0.06em;
		text-align: center;
	}

	/* BLOQUES DE SERVICIO */
	.servicio-bloque {
		padding-block: var(--section-gap);
	}

	.servicio-bloque--imagen-derecha,
	.servicio-bloque--imagen-izquierda {
		background: var(--bg-base);
	}

	.servicio-bloque--full {
		background: var(--bg-surface);
	}

	.servicio-bloque__inner {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: clamp(3rem, 6vw, 6rem);
		align-items: center;
	}

	.servicio-bloque__imagen {
		border-radius: var(--radius-lg);
		overflow: hidden;
		aspect-ratio: 4/3;
	}

	.servicio-bloque__imagen img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}

	.servicio-bloque__texto {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.servicio-num {
		font-family: var(--font-display);
		font-size: 4rem;
		line-height: 1;
		background: linear-gradient(
			135deg,
			#00cfdd 0%,
			#0a2fa8 35%,
			#e0004f 100%
		);
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
	}

	.servicio-titulo {
		color: var(--text-primary);
		line-height: 1.1;
	}

	.servicio-desc {
		font-size: 1.26rem;
		color: var(--text-secondary);
		line-height: 1.7;
	}

	.servicio-desc--wide {
		max-width: 65ch;
		margin-inline: auto;
		text-align: center;
	}

	.servicio-items {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		border-left: 2px solid rgba(0, 194, 255, 0.3);
		padding-left: 1.25rem;
	}

	.servicio-item__nombre {
		display: block;
		font-weight: 700;
		color: var(--text-primary);
		font-size: 1.14rem;
		letter-spacing: 0.04em;
		text-transform: uppercase;
		margin-bottom: 0.25rem;
	}

	.servicio-item__desc {
		font-size: 1.08rem;
		color: var(--text-secondary);
		line-height: 1.6;
	}

	/* Servicio full width */
	.servicio-full__header {
		text-align: center;
		margin-bottom: clamp(2rem, 4vw, 3rem);
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 1rem;
	}

	.servicio-full__grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 1.25rem;
		margin-bottom: 2.5rem;
	}

	.servicio-full__grid--3col {
		grid-template-columns: repeat(3, 1fr);
	}

	.servicio-item--card {
		background: var(--bg-card);
		border: 1px solid var(--empresa-dim);
		border-radius: var(--radius-lg);
		padding: 1.5rem;
		transition: border-color var(--t-med) var(--ease);
	}

	.servicio-item--card:hover {
		border-color: var(--empresa);
	}

	.servicio-item--card .servicio-item__nombre {
		color: var(--text-primary);
		margin-bottom: 0.5rem;
	}

	.servicio-full__cta {
		text-align: center;
	}

	.servicio-btn {
		background: #fff;
		color: #000;
		border: none;
	}

	.servicio-btn:hover {
		background: #fff;
		border: 2px solid var(--cyan);
		transform: translateY(-2px);
		background-clip: text;
		-webkit-background-clip: text;
		color: transparent;
		background-image: linear-gradient(90deg, var(--cyan), var(--magenta));
	}

	/* FRASE CIERRE */
	.cierre-sec {
		padding-block: clamp(4rem, 8vw, 7rem);
		background: var(--bg-surface);
		border-top: 1px solid var(--border);
		text-align: center;
	}

	.cierre-sec__texto {
		font-family: var(--font-display);
		font-size: clamp(1.6rem, 4vw, 3rem);
		letter-spacing: 0.05em;
		line-height: 1.15;
	}

	/* RESPONSIVE */
	@media (max-width: 900px) {
		.etapas__grid {
			grid-template-columns: 1fr;
		}

		.servicio-bloque__inner {
			grid-template-columns: 1fr;
		}

		.servicio-bloque--imagen-izquierda .servicio-bloque__imagen {
			order: -1;
		}

		.servicio-full__grid {
			grid-template-columns: 1fr;
		}

		.servicio-full__grid--3col {
			grid-template-columns: 1fr;
		}
	}

	/* SECCIÓN 5 — ASESORÍA ESTRATÉGICA */
	/* 5 — ASESORÍA */
	.asesoria-sec {
		background: var(--bg-base);
	}

	/* 5A — HEADER: texto izquierda / foto derecha */
	.asesoria-header {
		display: grid;
		grid-template-columns: 55% 45%;
		min-height: 80vh;
		border-top: 1px solid var(--border);
	}

	.asesoria-body {
		background: var(--bg-surface);
		display: flex;
		align-items: center;
		padding-block: clamp(4rem, 8vw, 7rem);
	}

	.asesoria-body__intro {
		margin-bottom: 3rem;
	}

	.asesoria-titulo {
		font-size: clamp(1.8rem, 4vw, 3.4rem);
		line-height: 1.05;
		margin-top: 0.75rem;
		margin-bottom: 1.5rem;
	}

	.asesoria-desc {
		font-size: 1.26rem;
		color: var(--text-secondary);
		line-height: 1.75;
		max-width: 58ch;
	}

	.asesoria-listas {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 2.5rem;
	}

	.asesoria-foto {
		width: 100%;
		height: 100%;
		min-height: 500px;
		overflow: hidden;
	}

	.asesoria-foto img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center 30%;
		display: block;
	}

	.asesoria-lista__titulo {
		font-family: var(--font-body);
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: 1rem;
		font-size: 0.8rem;
		text-transform: uppercase;
		letter-spacing: 0.04em;
		padding-bottom: 0.6rem;
		border-bottom: 1px solid var(--border);
	}

	.asesoria-lista ul {
		display: flex;
		flex-direction: column;
		gap: 0.6rem;
		padding: 0;
		list-style: none;
	}

	.asesoria-lista ul li {
		font-size: 1.14rem;
		color: var(--text-secondary);
		padding-left: 1.4rem;
		position: relative;
		line-height: 1.55;
	}

	.asesoria-lista ul li::before {
		content: "·";
		position: absolute;
		left: 0;
		color: var(--cyan);
		font-weight: 700;
		font-size: 1.3em;
		line-height: 1.1;
	}

	.asesoria-nota {
		color: var(--text-dim);
		font-style: italic;
		font-size: 0.875rem;
	}

	/* 5B — PRICING */
	.asesoria-pricing {
		background: var(--bg-base);
		padding-block: clamp(3rem, 5vw, 4.5rem);
		border-top: 1px solid var(--border);
		border-bottom: 1px solid var(--border);
	}

	.asesoria-pricing__inner {
		display: grid;
		grid-template-columns: 1fr auto 1fr auto 1fr;
		align-items: center;
		max-width: 860px;
		margin-inline: auto;
	}

	.asesoria-pricing__divider {
		width: 1px;
		height: 70px;
		background: linear-gradient(
			to bottom,
			transparent,
			var(--border),
			transparent
		);
		flex-shrink: 0;
		margin-inline: 3rem;
	}

	.asesoria-pricing__item {
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.6rem;
		text-align: center;
		padding: 1rem;
	}

	.asesoria-pricing__label {
		font-family: var(--font-display);
		font-size: 0.78rem;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--text-muted);
	}

	.asesoria-pricing__valor {
		font-size: 2rem;
		font-weight: 700;
		color: var(--text-primary);
		line-height: 1;
	}

	.asesoria-pricing__precios {
		display: flex;
		flex-direction: column;
		gap: 0.3rem;
		font-size: 1rem;
		color: var(--text-secondary);
		line-height: 1.5;
		white-space: nowrap;
	}

	.asesoria-pricing__precios strong {
		color: var(--cyan);
		font-size: 1.15rem;
	}

	/* 5C — PASOS */
	.asesoria-pasos {
		padding-block: var(--section-gap);
		background: var(--bg-surface);
	}

	.asesoria-paso {
		display: grid;
		grid-template-columns: 100px 1fr;
		gap: 3rem;
		align-items: start;
		padding-block: 3.5rem;
		border-bottom: 1px solid var(--border);
	}

	.asesoria-paso:first-child {
		padding-top: 0;
	}

	.asesoria-paso:last-child {
		border-bottom: none;
		padding-bottom: 0;
	}

	.asesoria-paso__num {
		font-family: var(--font-display);
		font-size: 5.5rem;
		line-height: 0.85;
		background: linear-gradient(
			135deg,
			#00cfdd 0%,
			#0a2fa8 35%,
			var(--empresa) 100%
		);
		-webkit-background-clip: text;
		background-clip: text;
		color: transparent;
		position: sticky;
		top: 140px;
	}

	.asesoria-paso__titulo {
		font-family: var(--font-body);
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: 0.75rem;
		text-transform: uppercase;
		letter-spacing: 0.04em;
	}

	.asesoria-paso__desc {
		font-size: 1.2rem;
		color: var(--text-secondary);
		line-height: 1.7;
		margin-bottom: 2rem;
		max-width: 65ch;
	}

	.asesoria-paso__btn {
		background: #fff;
		color: #000;
		border: none;
	}

	.asesoria-paso__btn:hover {
		background: #fff;
		border: 2px solid var(--cyan);
		transform: translateY(-2px);
		background-clip: text;
		-webkit-background-clip: text;
		color: transparent;
		background-image: linear-gradient(90deg, var(--cyan), var(--magenta));
	}

	/* FORM */
	.asesoria-paso__aviso {
		display: inline-flex;
		align-items: flex-start;
		gap: 0.6rem;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: var(--radius);
		padding: 0.75rem 1rem;
		font-size: 0.875rem;
		color: var(--text-dim);
		line-height: 1.5;
		max-width: 520px;
	}

	.asesoria-form--preview {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		max-width: 560px;
		background: var(--bg-card);
		border: 1px solid var(--border);
		border-radius: var(--radius-lg);
		padding: 1.75rem 2rem;
		margin-top: 0.5rem;
	}

	.asesoria-form__campo {
		display: flex;
		flex-direction: column;
		gap: 0.4rem;
	}

	.asesoria-form__label {
		font-size: 0.75rem;
		font-weight: 700;
		color: var(--text-secondary);
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	.asesoria-form__input-preview {
		background: var(--bg-base);
		border: 1px solid var(--border);
		border-radius: var(--radius);
		padding: 0.75rem 1rem;
		font-size: 0.9rem;
		color: var(--text-dim);
		font-style: italic;
		line-height: 1.4;
	}

	.asesoria-form__input-preview--tall {
		padding-block: 1rem;
		min-height: 80px;
	}

	/* 5D — NORMATIVAS */
	.asesoria-normativas {
		padding-block: clamp(3rem, 6vw, 5rem);
		background: var(--bg-base);
	}

	.asesoria-normativas__inner {
		background: var(--bg-card);
		border: 1px solid var(--empresa-dim);
		border-radius: var(--radius-lg);
		padding: clamp(2rem, 4vw, 3rem);
		max-width: 800px;
		margin-inline: auto;
		transition: border-color var(--t-med) var(--ease);
		position: relative;
		overflow: hidden;
	}

	.asesoria-normativas__inner::before {
		content: "";
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--empresa), transparent 70%);
	}

	.asesoria-normativas__inner:hover {
		border-color: var(--empresa);
	}

	.asesoria-normativas__titulo {
		font-family: var(--font-body);
		font-weight: 700;
		color: var(--text-primary);
		margin-bottom: 1.5rem;
		text-transform: uppercase;
		letter-spacing: 0.04em;
	}

	.asesoria-normativas__lista {
		display: flex;
		flex-direction: column;
		gap: 1rem;
		list-style: none;
		padding: 0;
	}

	.asesoria-normativas__lista li {
		font-size: 1.08rem;
		color: var(--text-secondary);
		padding-left: 1.5rem;
		position: relative;
		line-height: 1.65;
	}

	.asesoria-normativas__lista li::before {
		content: "·";
		position: absolute;
		left: 0;
		color: var(--empresa);
		font-weight: 700;
		font-size: 1.4em;
		line-height: 1;
		top: 0.1em;
	}

	/* 5E — CIERRE */
	.asesoria-cierre {
		padding-block: clamp(5rem, 10vw, 9rem);
		background: var(--bg-base);
		text-align: center;
		border-top: 1px solid var(--border);
		position: relative;
		overflow: hidden;
	}

	.asesoria-cierre::before {
		content: "";
		position: absolute;
		inset: 0;
		background: radial-gradient(
			ellipse at 50% 100%,
			rgba(181, 0, 95, 0.07) 0%,
			transparent 65%
		);
		pointer-events: none;
	}

	.asesoria-cierre__inner {
		position: relative;
		z-index: 1;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 3rem;
	}

	.asesoria-cierre__titulo {
		line-height: 1.1;
		max-width: 20ch;
		margin-inline: auto;
	}

	@media (max-width: 900px) {
		.asesoria-header {
			grid-template-columns: 1fr;
		}

		.asesoria-foto {
			min-height: unset;
			aspect-ratio: 16/9;
			height: auto;
		}

		.asesoria-titulo {
			font-size: 1.62rem;
		}

		.asesoria-listas {
			grid-template-columns: 1fr;
		}

		.asesoria-pricing__inner {
			grid-template-columns: 1fr;
			justify-items: center;
			gap: 0;
		}

		.asesoria-pricing__divider {
			width: 60px;
			height: 1px;
			margin: 1.5rem auto;
		}

		.asesoria-paso {
			grid-template-columns: 1fr;
			gap: 1rem;
		}

		.asesoria-paso__num {
			font-size: 3.5rem;
			position: static;
		}

		.asesoria-form--preview {
			padding: 1.25rem;
		}
	}
</style>
